export default function AuthorApplicationBanner() {
  // Author applications are now opened from the stable "Are you an author?" button
  // in the main navigation. The old floating banner is intentionally disabled.
  return null
}
