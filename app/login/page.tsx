"use client";import Link from "next/link";import {useState} from "react";
export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setMsg("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});const x=await r.json();if(x.ok)location.href="/account";else setMsg(x.error||"Sign in failed");}return <><header className="site-header"><div className="wrap nav"><Link className="brand" href="/">Shoppers Ocean</Link><nav><Link href="/">Home</Link><Link href="/bookShelf">Bookshelf</Link></nav></div></header><main className="wrap"><form className="form" onSubmit={submit}><h1>Sign in</h1><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="button primary">Sign in</button><p>No OTP or phone verification.</p>{msg&&<p>{msg}</p>}<Link href="/register">Create account</Link></form></main></>}