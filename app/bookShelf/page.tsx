"use client"; import Link from "next/link"; import {useEffect,useState} from "react";
type Book={id:string;title:string;author?:string;language?:string;category?:string;description?:string;cover_url?:string;price?:number};
export default function BookShelf(){const[books,setBooks]=useState<Book[]>([]);const[q,setQ]=useState("");const[loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/books").then(r=>r.json()).then(x=>setBooks(x.books||[])).finally(()=>setLoading(false))},[]);const filtered=books.filter(b=>[b.title,b.author,b.language,b.category].join(" ").toLowerCase().includes(q.toLowerCase()));return <><header className="site-header"><div className="wrap nav"><Link className="brand" href="/">Shoppers Ocean</Link><nav><Link href="/">Home</Link><Link href="/login">Sign in</Link></nav></div></header><main className="wrap section"><h1>Bookshelf</h1><div className="filters"><button className="button primary">Categories</button><button className="button">All languages</button><button className="button">All authors</button><input className="search" placeholder="Search books" value={q} onChange={e=>setQ(e.target.value)}/></div>{loading?<div className="empty"><h2>Loading books…</h2></div>:filtered.length===0?<div className="empty"><h2>No books yet</h2><p>The new catalogue starts fresh. Add books from the admin system.</p></div>:<div className="book-grid">{filtered.map(b=><article className="book-card" key={b.id}>{b.cover_url&&<img src={b.cover_url} alt="" /> }<h2>{b.title}</h2><p>{b.author}</p><small>{b.language} · {b.category}</small><div><Link className="button primary" href={`/book/${b.id}`}>Read as flipbook</Link></div></article>)}</div>}</main></>}