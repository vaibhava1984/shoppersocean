import {getCloudflareContext} from "@opennextjs/cloudflare"; import crypto from "node:crypto";
export async function POST(req:Request){const b=await req.json(); if(!b.email||!b.password)return Response.json({error:"Email and password required"},{status:400}); const {env}=await getCloudflareContext({async:true}); const hash=crypto.createHash("sha256").update(b.password).digest("hex"); const u=await env.DB.prepare("SELECT id,name,email,role FROM users WHERE email=? AND password_hash=?").bind(b.email.toLowerCase(),hash).first(); if(!u)return Response.json({error:"Invalid email or password"},{status:401}); return Response.json({ok:true,user:u});}