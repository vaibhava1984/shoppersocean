"use client"; import Link from "next/link"; import {useState} from "react";
export default function Account(){const[id,setId]=useState(""),[rows,setRows]=useState<any[]>([]),[msg,setMsg]=useState("");async function load(){const r=await fetch("/api/purchases?user_id="+encodeURIComponent(id));const x=await r.json();setRows(x.purchases||[]);setMsg(x.error||"")}return <><header className="site-header"><div className="wrap nav"><Link className="brand" href="/">Shoppers Ocean</Link><nav><Link href="/bookShelf">Bookshelf</Link></nav></div></header><main className="wrap section"><h1>My purchases</h1><div className="form"><input placeholder="Account ID" value={id} onChange={e=>setId(e.target.value)}/><button className="button primary" onClick={load}>Load purchases</button>{msg&&<p>{msg}</p>}</div><div className="cards">{rows.map(r=><div key={r.id}><h3>{r.title}</h3><p>{r.status} · ₹{r.amount}</p></div>)}</div></main></>}