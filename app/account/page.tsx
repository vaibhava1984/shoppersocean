"use client";import Link from "next/link";import {useEffect,useState} from "react";
export default function Account(){const[user,setUser]=useState<any>(null);const[rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch("/api/auth/me").then(r=>r.json()).then(x=>{setUser(x.user);if(x.user)fetch("/api/purchases").then(r=>r.json()).then(y=>setRows(y.purchases||[]))})},[]);async function out(){await fetch("/api/auth/logout",{method:"POST"});location.href="/";}return <><header className="site-header"><div className="wrap nav"><Link className="brand" href="/">Shoppers Ocean</Link><nav><Link href="/bookShelf">Bookshelf</Link></nav></div></header><main className="wrap section"><h1>My Account</h1>{user?<><p>Welcome, {user.name}.</p><p>{user.email}</p><div className="actions"><Link className="button" href="/bookShelf">Bookshelf</Link><button className="button primary" onClick={out}>Sign out</button></div><section className="section"><h2>My purchases</h2><div className="cards">{rows.map(r=><div key={r.id}><h3>{r.title}</h3><p>{r.status} · ₹{r.amount}</p></div>)}</div></section></>:<p>Please <Link href="/login">sign in</Link>.</p>}</main></>}